extends layout 

block content 
    .row.banner     
        .col-12 
            h1= pageHeader.title 
    .row    
        .col-12.col-md-8
            p !{(content).replace(/\n/g, '<br/>')}
            